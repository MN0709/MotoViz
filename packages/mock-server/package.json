{
  "name": "@motofit/mock-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npm run build && node --enable-source-maps dist/index.js",
    "start": "node --enable-source-maps dist/index.js",
    "test": "npm run build && node --test dist/routes/rag.test.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@motorcycle-ai/shared": "0.1.0",
    "express": "^5.1.0",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/multer": "^2.0.0"
  }
}
