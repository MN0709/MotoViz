{
  "name": "motorcycle-ai",
  "version": "0.1.0",
  "private": true,
  "description": "摩装智舱（MotoFit AI）黑客松 Monorepo",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@motorcycle-ai/web",
    "dev:mock": "npm run dev --workspace=@motofit/mock-server",
    "test:references": "npm run build --workspace=@motorcycle-ai/shared && npm run test --workspace=@motorcycle-ai/web",
    "demo:diagnose": "npm run build --workspace=@motorcycle-ai/shared && npm run demo:diagnose --workspace=@motorcycle-ai/rag-engine",
    "demo:interactive": "npm run build --workspace=@motorcycle-ai/shared && npm run demo:interactive --workspace=@motorcycle-ai/rag-engine",
    "test:mock": "npm run build --workspace=@motorcycle-ai/shared && npm run test --workspace=@motofit/mock-server",
    "test:rag": "npm run build --workspace=@motorcycle-ai/shared && npm run test --workspace=@motorcycle-ai/rag-engine",
    "build": "npm run build --workspace=@motorcycle-ai/shared && npm run build --workspaces --if-present",
    "lint": "eslint . --ext .ts,.tsx --max-warnings=0",
    "typecheck": "npm run build --workspace=@motorcycle-ai/shared && npm run typecheck --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react-hooks": "^5.0.0",
    "jsdom": "^26.1.0",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
