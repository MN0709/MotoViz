# 分级验收：不同行的规则是 AND 关系（都必须批准），同一行多个 owner 是 OR 关系（只需一人批准）
# 3D组代码：3D组长 + 大组长 都必须批准
/packages/3d-renderer/**   @siguadht
# RAG组代码：RAG组长 + 大组长 都必须批准
/packages/rag-engine/**    @MN0709
# 所有代码：大组长必须批准（公共/前端文件只匹配这一行）
*                         @moronfranklyn-lab
